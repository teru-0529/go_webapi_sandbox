version: "3.9"
services:
  app:
    image: gotodo
    build:
      args:
        - target=dev
    environment:
      TODO_ENV: dev
      PORT: ${API_PORT}
    volumes:
      - .:/app
    ports:
      - "${RELEASE_PORT}:${API_PORT}"
